#! /usr/bin/env node

// 1. Install recommended exts that are not there yet
require('./install-recommended-exts')

// 2. Offer to copy recommended settings as User Settings
require('./copy-user-settings')
